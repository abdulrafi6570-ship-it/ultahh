{
  "buildCommand": "pnpm --filter @workspace/birthday-os run build",
  "outputDirectory": "artifacts/birthday-os/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

